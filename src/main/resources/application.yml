server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://157.15.202.187:3306/tracker?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    username: root
    password: )u___peDah_A9qG
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: false

security:
  api-key: mySuperSecretKey123456